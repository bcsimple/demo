
name: this is a demo
on: workflow_dispatch

jobs:
  build:
    services:
      nginx:
        # Docker Hub image
        image: nginx:alpine
        ports:
          - 8080:80
    steps:
    - name: curl
      run: curl localhost:8080
 
